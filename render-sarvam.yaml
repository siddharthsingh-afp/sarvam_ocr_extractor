services:
  - type: web
    name: affordplan-sarvam-ocr
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT
    healthCheckPath: /health
    envVars:
      - key: SARVAM_API_KEY
        sync: false
